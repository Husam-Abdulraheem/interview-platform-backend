# Copilot Instructions

## Project Guidelines
- User's workspace targets .NET 8 and uses Visual Studio 2026; prefer pwsh.exe as terminal.
- User prefers to keep the custom PBKDF2-based PasswordHasher implementation (not switching to ASP.NET Identity).